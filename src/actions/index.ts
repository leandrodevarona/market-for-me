import { cart } from "./cart";
import { markets } from "./markets";
import { products } from "./products";

export const server = {
  // Mercados
  markets,
  // Productos
  products,
  // Carrito de compras
  cart,
};
